{
  "issue": "https://github.com/manuel71sj/SignalAhead/issues/6",
  "status": "honest-unverified-normalization-verified-live-collector-disabled",
  "artifacts": {
    "normalizer": "services/api/src/providers/national/normalize.ts",
    "types": "services/api/src/providers/national/types.ts",
    "budget": "services/api/src/collection/requestBudget.ts",
    "sharedRequests": "services/api/src/collection/sharedRequests.ts",
    "eventCache": "services/api/src/collection/signalEventCache.ts",
    "tests": [
      "services/api/test/providers/national.test.ts",
      "services/api/test/collection/collection.test.ts"
    ]
  },
  "decisions": {
    "timeUnit": "National remainingAtSourceMs and expiresAtUtcMs are null. Raw remaining values remain strings in separate parse-result diagnostics; no raw value is represented as milliseconds without verified unit evidence.",
    "sourceTime": "National sourceEventId and sourceObservedAtUtcMs are null, sourceRevision is unknown, and sourceTimeKind is unknown. Raw totDt is preserved only in diagnostics, not fabricated into an event identifier or lexically sortable timestamp.",
    "sourceIdentity": "National sourceIntersectionId preserves stdgCd + ':' + crsrdId.",
    "direction": "Raw direction prefixes remain diagnostic values and parts of candidate source approach keys; they confer no operational road-direction approval.",
    "requestSharing": "Request planning groups same-region subscriptions; no live collection loop is enabled.",
    "budget": "Existing request budgeting requires configured daily and per-second limits; it does not prove an approved provider quota exists.",
    "cache": "Canonical observations are schema-validated. Duplicate receipt does not extend expiry. Only verified source generation times establish order; opaque event IDs never do. Conflicting unknown-order replacement loses prediction timing. Cache cardinality is bounded at 10000."
  },
  "verification": {
    "status": "passed-local-and-pinned-compose-runtime",
    "results": "API typecheck/build and all 37 API tests passed, including normalizer/cache regressions. No live provider request was made during this runtime repair.",
    "commands": [
      "npm run check:api",
      "npm --workspace services/api test -- test/providers/national.test.ts test/collection/collection.test.ts"
    ]
  },
  "notClosedReasons": [
    "National source unit, source time semantics and operational direction mapping remain unverified.",
    "No live operational collector, approved provider quota or Redis signal distribution is implemented/enabled.",
    "The in-process signal cache is not evidence of an operational provider feed."
  ]
}
